<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
      Cross-language E2E: Java server that the Go / Rust rmtt clients connect
      to. Depends on the published rmtt-java 1.0.1 artifacts pulled from Maven
      Central, so this project builds standalone (no local rmtt-java checkout
      required).
    -->
    <parent>
        <groupId>net.czqu.rmtt</groupId>
        <artifactId>rmtt-java-parent</artifactId>
        <version>1.0.1</version>
    </parent>

    <artifactId>rmtt-e2e-java-server</artifactId>
    <name>rmtt-e2e-java-server</name>
    <description>Java rmtt server entry point for the cross-language E2E matrix
        (echoes every upstream push back to the connecting client).</description>

    <properties>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>net.czqu.rmtt</groupId>
            <artifactId>rmtt-server-netty</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- The parent enables strict javadoc + sources jars; skip them here:
                 this is a test harness, not a published artifact. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <configuration>
                    <skipSource>true</skipSource>
                </configuration>
            </plugin>
            <!-- Self-contained runnable jar for the CI / remote harness. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <finalName>rmtt-e2e-java-server</finalName>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>net.czqu.e2e.server.E2eServerMain</mainClass>
                                </transformer>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
